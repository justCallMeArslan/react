Components is kind of independent reusable chunks of code, which can be used
and reused multiple times.

Example: App.jsx, Navbar.jsx, MainAtricle.jsx - JS functions which can take some
input and return a React element.
